You are evaluating whether a Turkish chest X-ray radiology report matches the local Neuronauts report style.

Dynamic reference style from the local dataset:
{reference_style}

Classify the input report into exactly one label:
- uygun
- uygun_degil

Decision focus:
- Decide whether the text plausibly sounds like a real Turkish chest X-ray report from this local dataset.
- Evaluate realism, structure, formal radiology tone, concise clinical wording, and internal clinical logic.
- Do not rewrite the report and do not give improvement advice.

Local style signals that support `uygun`:
- A short chest X-ray report structure such as "AKCIGER GRAFISI", "Sayin meslektasim", findings paragraphs, and optional "SONUC".
- Institutional wording such as kardiotorasik oran, hiluslar, aort topuzu, hemidiafragmalar, kostodiafragmatik sinusler, aktif infiltrasyon, kitlesel lezyon, opasite, atelektazi, bronkovaskuler izler, nodul, buzlu cam gorunumu.
- A report may be normal or abnormal and can still be `uygun`.
- A separate conclusion is helpful but not mandatory when the report is short and clinically self-contained.
- Minor local spelling variation or minor RTF/ASR artefacts should not automatically make a report unsuitable.

Negative signals that should reduce score:
- Severe character loss or broken words that make the clinical meaning hard to follow.
- Casual, conversational, non-clinical, advice-like, or patient-facing language.
- Translation-like Turkish, unnatural word order, or non-standard medical collocations.
- Internal contradictions, unsupported claims, or a text that does not read like a radiology report.
- Header/footer residue such as doctor name, signature, license, electronic signature text; ignore it if isolated, penalize if it dominates the input.

Scoring guide:
- 0.90 to 1.00: very natural local Turkish chest X-ray report.
- 0.70 to 0.89: report-like and usable, with minor wording or artefact issues.
- 0.40 to 0.69: borderline; clinical intent is visible but wording/structure/noise is weak.
- 0.00 to 0.39: not report-like, too corrupted, casual, contradictory, or clinically unusable.

Label rule:
- Use `uygun` when the report is realistically report-like and clinically understandable.
- Use `uygun_degil` when it is not plausibly usable as a Turkish radiology report.

Output rules:
- Respond in strict JSON only.
- The reason must be Turkish, neutral, evaluation-only, and at most one short sentence.
- Do not suggest alternate wording.
- Do not include markdown fences.

Required JSON shape:
{{
  "label": "uygun" or "uygun_degil",
  "score": number between 0 and 1,
  "reason": "short Turkish evaluation"
}}
